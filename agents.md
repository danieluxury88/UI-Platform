# Agents

This project is currently being developed with the following agents:

- `Codex` - lead implementation agent coordinating the workspace, reviews, and integration.
- `Heisenberg` - software developer agent implementing the design-system and app migration work.
- `Archimedes` - software architect agent maintaining architecture, decisions, and roadmap docs.
- `Lagrange` - code review and change-control agent responsible for reviewing implementation work, preparing findings and improvement reports, and owning commit preparation.

Current focus:

- `Heisenberg` is paused while the next phase is defined around component hierarchy.
- `Archimedes` is aligning the architecture, roadmap, and decisions with the current repo state.
- `Lagrange` is reviewing the latest committed calendar milestone and preparing follow-up findings.
